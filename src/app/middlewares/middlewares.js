/**
 * Roda entre a requisição e o controller.
 *
 * Um uso comum é a autenticação de usuários ou a inclusão
 * de dados extras à requisição inicial.
 *
 * export default async (req, res, next) => {}
 */
